# Placeholder — Step 5 builds functions_sil here as the C-API DLL (functions_interface_c.h/.cpp,
# plan.md §5.6). ROS2 is a separate, last-priority src/platform/ros2/ target, not this one.
add_library(functions_sil INTERFACE)
